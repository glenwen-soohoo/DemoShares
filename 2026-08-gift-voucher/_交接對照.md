# 交接對照 — 2026-08-gift-voucher

> 建立日期：2026-09-18
> 類型：參考資料
> 資料來源：`D:\GreenBox\fruit_web` 分支 `feature/DEV-3963-gift-voucher-module`（相對 `dev` 共 71 個 commit）
> 最後更新：2026-09-18（Obsidian `0_開發文件/` 已全數更正；C 區標 ✅ 的是已修）
> 適用對象：接手這個 demo 的人，以及要照 demo 改程式的 AI

---

## 先看這一段：名字換過兩次，demo 與規劃文件都落後

同一件東西在四個地方有三種名字：

| 出處 | 用的名字 | 狀態 |
| --- | --- | --- |
| Obsidian `0_開發文件/01`、`03`、`05`、`06` | `GiftItem` / `IsGiftItem` / `GiftVoucher*` / `GiftVoucherService` | **過時** |
| Notion 開發文件 `01`、`06`（2026-09-09 後改的） | `GiftVoucherItem` / `IsGiftVoucherItem` / `GiftVoucherOption` | **過時** |
| 本 demo（改名前） | `GiftItem` / `GiftVoucher*` | **已於 2026-09-18 改掉** |
| **`feature/DEV-3963-gift-voucher-module`（實際程式碼）** | **`PresentItem` / `IsPresentItem` / `PresentVoucher*`** | **唯一真相** |

緣由寫在 migration 的檔頭註解（`GreenBox.DbMigrations/Scripts/20260909_DEV-3963_2_presentvoucher-tables.sql`）：2026-09-09 先決定不動既有贈品系統、新功能改用 `GiftVoucher` 前綴；2026-09-10 PM 再定名「禮物 Present／禮物券 Present Voucher」，於是全部換成 `Present*`。Notion 開發文件 07 記的是 09-09 那一版，沒跟上 09-10 的改名。

**所以：看 Obsidian 或 Notion 的舊敘述時要自己換算，欄位名以本文件與實際程式碼為準。**

---

## A. 現有真名（demo 已對齊，工程直接抄）

### A-1　資料表

| demo 原本寫 | 真實名稱 | 來源 |
| --- | --- | --- |
| `GiftItem` | `dbo.PresentItem` | `20260909_DEV-3963_2_presentvoucher-tables.sql:88` |
| `GiftItemCandidate` | `dbo.PresentItemCandidate` | 同上:143 |
| `GiftVoucherType` | `dbo.PresentVoucherType` | 同上:187 |
| `GiftVoucher` | `dbo.PresentVoucher` | 同上:244 |
| `GiftVoucherItem`（券的候選禮物） | `dbo.PresentVoucherOption` | 同上:356 |
| `GiftVoucherLog` | `dbo.PresentVoucherLog` | 同上:392 |
| （demo 沒寫） | `dbo.MemberBaby` | `20260909_DEV-3963_3_memberbaby.sql:622` |

### A-2　欄位

| demo 原本寫 | 真實名稱 | 備註 |
| --- | --- | --- |
| `GiftName` | `PresentItem.PresentName` | |
| `MemberId`（持有會員） | `PresentVoucher.VolunteersId` | **撈資料最常踩**。會員主檔是 `dbo.Volunteers`，對它的 `Id` |
| `IsGiftItem` | `ProductDetail.IsPresentItem` | `20260909_DEV-3963_1` 加的。⚠ 不在 `ProductDetailMainView` 的欄位清單裡，走 view 的查詢看不到 |
| `GiftItem.ProductDetailId` | `PresentItem.ProductDetailId` | 1:1 unique |
| （demo 的「會員表名還沒對過」） | `dbo.Volunteers` | |

`PresentItem` 的其餘欄位：`PresentImage`、`PresentDescription`（禮物本身的介紹）、`ActivityNote`（活動說明，前台彈窗顯示的是這一欄）、`CartLimitType`、`DisplayPrice`、`IsActive`。

`PresentVoucher` 的欄位與 demo 的 `data-schema.html` 一致：`VoucherNo`、`VoucherTypeId`、`Status`、`IssuedTime`、`ExpireTime`、`UsedTime`、`UsedOrderNumber`、`SnapshotProductId` / `SnapshotProductDetailId` / `SnapshotProductName` / `SnapshotSpecDesc`、`Source`、`SourceRefId`、`IssuedBy`、`IssueReason`、`Remark`。

`MemberBaby`：`VolunteersId`、`Nickname`、`Birthday`、`CreateTime`、`UpdateTime`、`UpdatedBy`、`BirthdayVoucherId`、`BirthdayPresentSentTime`、`BirthdaySmsSent`、`IsDeleted`。

### A-3　表單欄位名（後台建單頁 / 會員編輯頁）

| demo 原本寫 | 真實名稱 | 來源 |
| --- | --- | --- |
| `name="IsMember"`　值 **1 會員 / 0 非會員** / 2 建新 | `name="MemberMode"`　值 **0 會員 / 1 非會員** / 2 同步建新會員 | `CreatePayment.cshtml:239-241` |
| `name="recv"`（會員頁收件時間） | `name="ReciveTime"`（`Volunteers.ReciveTime`，注意是 `Recive` 不是 `Receive`） | `Volunteers/Edit.cshtml:498-500` |
| 預設出貨備註 textarea（無 name） | `name="OrderRemarks"`（`Volunteers.OrderRemarks`） | `Volunteers/Edit.cshtml:511` |
| 預設倉庫備註 textarea（無 name） | `name="Remarks"`（`Volunteers.Remarks`） | `Volunteers/Edit.cshtml:522` |
| `name="ReceiveTime"`（建單頁配達時段） | 一樣 `ReceiveTime` | `CreatePayment.cshtml:355-357`，走 `Html.RadioButtonFor` |

> ⚠️ **`MemberMode` 的 0 與 1 跟 demo 原本裝反了。** demo 已改成真實對應（連 JS 的判斷式一起改），照舊 demo 寫程式會把「會員」與「非會員」整個對調。

### A-4　路徑 / API

| 用途 | 真實路徑 | 來源 |
| --- | --- | --- |
| 禮物券管理後台（列表／明細） | `/GoX/PresentVoucher/Index`、`/GoX/PresentVoucher/Edit` | `Areas/GoX/Controllers/PresentVoucherController.cs:48,62` |
| 後台其餘動作 | `Query` / `GetEditData` / `GetProductBindingInfo` / `Create` / `Update` / `SetActive` / `SetCandidates` / `GetTypes` / `GetStats` / `GetBirthdayRuleInfo` / `CreateType` / `UpdateType` / `DeleteType` / `IssueToAccounts` / `UpdateStock` | 同上 |
| 前台會員中心列券 | `GET /api/PresentVoucherApi/GetMyVouchers` | `Controllers/Api/PresentVouchers/PresentVoucherApiController.cs:59` |
| 前台券加入購物車 | `POST /api/PresentVoucherApi/AddToCart` | 同上:122 |
| 訂單頁返還禮物券 | `POST /GoX/Orders/RefundPresentVoucher` | `Areas/GoX/Controllers/OrdersController.cs:10218` |
| **E-03b 配送區判定** | `GET /GoX/Orders/DeliveryZoneByAddress?address=` | `Areas/GoX/Controllers/OrdersController.cs:7378` |
| E-04 複製訂單 | `GET /GoX/Orders/CopyOrderItems?orderNumber=&volunteersId=` | 同上:7239 |
| E-04b 匯入文字檔 | `GET /GoX/Orders/ImportTextProducts?text=` | 同上:5381 |
| （前台既有）雙北親送判定 | `GET /Orders/CheckSameDay?county=&district=` | `Controllers/OrdersController.cs:6406` |

> ⚠️ **E-03b 的做法跟規格書寫的不一樣。** 規格書（E 規格、Notion 06）說「接現成的 `GET /Orders/CheckSameDay?county=&district=`」。那支確實存在，但工程師另外做了 `DeliveryZoneByAddress`，理由是建單頁的地址是**單一自由文字欄**，沒有分開的縣市／區欄位可以傳，所以判定放後端從字串反推（`order-create-payment.js:366`）。**以 `DeliveryZoneByAddress` 為準。**

### A-5　程式檔案

| 層 | 真實檔案 |
| --- | --- |
| 實體 | `GreenBox.Persistence/Data/PresentVouchers/PresentVoucherEntities.cs`、`Data/Entities/MemberBaby.cs` |
| DataContext | `Data/HuashanCRMDataContext.PresentVouchers.cs`、`.MemberBaby.cs` |
| DTO | `GreenBox.Persistence/Models/PresentVouchers/*`、`Models/MemberBabies/*` |
| Repository | `GreenBox.Infrastructure/Repository/PresentVoucherUnit/*`、`MemberBabyUnit/*` |
| Service | `GreenBox.Service/Implementation/PresentVouchers/{PresentVoucherService,PresentItemService,PresentVoucherRules}.cs`、`MemberBabies/{MemberBabyService,BabyBirthdayRules}.cs` |
| 後台 Controller | `HuashanCRM/Areas/GoX/Controllers/PresentVoucherController.cs` |
| 前台 API | `HuashanCRM/Controllers/Api/PresentVouchers/PresentVoucherApiController.cs` |
| 排程 | `HuashanCRM/Models/MemberBabies/BabyBirthdayPresentBatch.cs`、`Models/PresentVouchers/PresentVoucherReleaseHelper.cs` |
| 測試 | `Fruit_UnitTest/ServiceTests/{PresentVoucherServiceTests,BabyBirthdayRulesTests,OrderFinishBabyEntryTests}.cs` |

### A-6　demo 檔 ↔ 真實 view

| demo | 真實 view / 樣式 |
| --- | --- |
| `admin-gift-list.html`、`admin-gift-create.html`、`admin-gift-detail.html` | `Areas/GoX/Views/PresentVoucher/Index.cshtml`、`Edit.cshtml` |
| `admin-order-create-gift-c.html` | `Areas/GoX/Views/Orders/CreatePayment.cshtml`（＋`_CartItems`、`_OrderCoupon`、`_OrderAmountSection`）、`Scripts/Admin/Web/Orders/order-create-payment.js`、`Content/SCSS/Gox/OrderCreatePayment.scss` |
| `admin-member-gift-b.html` | `Areas/GoX/Views/Volunteers/Edit.cshtml`、`Content/SCSS/Gox/VolunteerEdit.scss` / `VolunteerEditFields.scss` / `VolunteerEditPresentVoucher.scss` |
| `admin-order-refund-gift.html` | `Areas/GoX/Views/Orders/`（返還掛在既有「返還訂單優惠」）＋ `OrdersController.RefundPresentVoucher` |
| `admin-product-spec.html` | React `ProductDetailSettings.tsx`（`react-component-export`） |
| `member-center-c.html` | `Views/Volunteers/Index.cshtml`、`Content/SCSS/Volunteers/{Index,MemberAreaShell,MemberCenterContent}.scss` |
| `member-baby-profile-b.html` | `Views/Volunteers/Change.cshtml`、`Content/SCSS/Volunteers/MemberProfile.scss` |
| `member-share-link.html` | `Views/Volunteers/ShareLink.cshtml`、`Content/SCSS/Volunteers/MemberShareLink.scss` |
| `checkout-cart.html` | `Views/Shared/_ConfirmProduct.cshtml`、`Scripts/Web/Orders/ConfirmOrder.js` |
| `checkout-finish-baby.html` | `Views/Shared/_PartialOrderFinish.cshtml`、`Views/Orders/FG*Finish.cshtml` |

### A-7　常數值（demo 已對，不用改）

| 項目 | 值 | 來源 |
| --- | --- | --- |
| 生日禮券種代碼 | `BD-BABY` | `PresentVoucherServiceTests.cs:100` |
| 券號格式 | `BD-BABY-0052`（券代碼 ＋ 4 位流水，超過四位不補零） | `PresentVoucherRules.BuildVoucherNo` |
| 發券來源 | `Source = "BabyBirthday"` | `BabyBirthdayRules.IssueSource` |
| 券狀態 | 0 未使用／1 已使用／2 已作廢，**沒有「已過期」** | migration:244 |
| 訂單欄位 | `direction`、`Del`、`OrdersStates`、`OrderNumber`、`TotalPrice`、`VolunteersId` | `HuashanCRM.designer.cs:86660` 起 |

---

## B. 每一頁開頭的真名註解

**每一份 demo 的 `<head>` 第一行是一段註解**，寫著這一頁對應正式站的哪個 view／JS／SCSS、
這一頁用到的真實欄位名與 API 路徑。打開檔案就看得到，不必先翻這份對照表。

其餘改不動、只能在內文標註的：

| demo 裡 | 為什麼不改 | 處理 |
| --- | --- | --- |
| `renderZone()` / `isIsland()`（前端判定配送區） | demo 是靜態頁，沒有後端可打。真實做法是後端反推 | ✅ 已在配達時段那一行上方加註解錨，指到 `DeliveryZoneByAddress` |
| `pickMember()` / `pickReason()`（多帳號挑人） | demo 的假資料池與真實查詢結構不同 | 沒加註解。邏輯本身正確（最近登入 ⇒ 最近下單 ⇒ 最早註冊），照抄判斷順序即可 |
| 各頁的 `oc-*` / `mcard-*` / `pv-*` class | demo 自己的樣式命名，真實 SCSS 已各自成檔 | 沒加註解也不對齊，看 A-6 與各頁 `<head>` 的 SCSS 對照 |

---

## C. 規劃文件要更正的（**這幾條會把工程端的 AI 帶歪**）

✅ ＝ 2026-09-18 已改完。Notion 那三列 Glen 指示這輪先不動。

| 文件 | 寫錯什麼 | 正確 |
| --- | --- | --- |
| ✅ Obsidian `01_禮物券機制.md`（全篇） | `GiftItem` / `GiftItemCandidate` / `GiftVoucherType` / `GiftVoucher` / `GiftVoucherItem` / `GiftVoucherLog` / `IsGiftItem` / `GiftItemService` / `GiftVoucherService` | 全部改 `Present*`；`GiftVoucherItem` ⇒ `PresentVoucherOption` |
| ✅ Obsidian `01` 3-2 | 欄位 `GiftDescription` | `PresentDescription` |
| ✅ Obsidian `01` 3-3 | `GiftItemId` / `CandidateGiftItemId` | `PresentItemId` / `CandidatePresentItemId` |
| ✅ Obsidian `03_粥寶生日禮.md`:143 | `BirthdayGiftSentTime` | `BirthdayPresentSentTime` |
| ✅ Obsidian `03`:147 | 索引 `VolunteerId` | `VolunteersId` |
| ✅ Obsidian `03`:161,212 | `BabyProfileService.cs` | `MemberBabyService.cs` |
| ✅ Obsidian `03`:163 | `GreenBox.Domain/Dtos/MemberBaby/*.cs` | `GreenBox.Persistence/Models/MemberBabies/*.cs` |
| ✅ Obsidian `03`:27 | `GiftVoucherService.Issue` | `PresentVoucherService.Issue`（`IssuePresentVoucherRequest`） |
| ✅ Obsidian `05`:73 | 「資料表 `GiftVoucherItem` 已預留」 | `PresentVoucherOption` |
| ✅ **Obsidian `05` 的「多列商品」那列** | 「`item2[n]` 在伺服器端沒有對應的 model binding」 | **已經不成立**。E-12 收成一列之後是 `item2[0].ProductName` / `item2[0].amount`，有完整 binding 與驗證（`CreatePayment.cshtml:432,438`）。要改成「固定一列，且該列有 model binding」 |
| ✅ Obsidian `06`:122 | `GiftItem.ActivityNote` / `GiftItem.GiftDescription` | `PresentItem.ActivityNote` / `PresentItem.PresentDescription` |
| ✅ Obsidian `06`:255,324 | `GiftItem.DisplayPrice` | `PresentItem.DisplayPrice` |
| ✅ Obsidian `06`:536,538 | `IsGiftItem`、`GiftItem.ProductDetailId` | `IsPresentItem`、`PresentItem.ProductDetailId` |
| ✅（Obsidian 部分）Obsidian `06` 第十節 E-03b、Notion 06 同段 | 「接現成的 `GET /Orders/CheckSameDay?county=&district=`」 | 實作走 `GET /GoX/Orders/DeliveryZoneByAddress?address=`（理由見 A-4） |
| Notion `01`、`06` | `GiftVoucherItem` / `IsGiftVoucherItem` / `GiftVoucherOption` | 同樣全部 `Present*` |
| Notion `07_命名決策` | 記的是 2026-09-09 的 `GiftVoucher` 前綴 | 要補 2026-09-10 PM 定名 `Present` 這一輪 |
| ✅ Obsidian `06` §一 主按鈕 | 卡片主按鈕寫「加入菜籃」 | 卡片是「使用」，「加入菜籃」是彈窗的確認鈕（demo 一直是這樣）|
| ✅ Obsidian `06` §四 條 3 | 「數量固定 1、不可修改」 | 數量不可改但不一定是 1：＝這份禮物佔用的券數，多張合併成一列、標籤寫「禮物券兌換 ×N」（`_ConfirmProduct.cshtml:343`、`Cart.cs` 的 `MarkAsPresentItem`）|
| ✅ Obsidian `06` §四 | 沒有「已停止贈送」這個標籤 | 禮物被下架（`PresentItem.IsActive = false`）時購物車那一列要標並擋結帳（`65eac8b92`）|

---

## D. 工程已經做完、規格書沒記的決定

這幾件是看 commit 才知道的，補記在這裡：

| 項目 | 實際做法 | commit |
| --- | --- | --- |
| **優惠券產線限制** | 面額型優惠券的產線限制**真的生效**（2026-09-18 Glen 追認）。判準是「車裡至少要有一項符合的產線」，與前台 `IsPassCouponCheck` 一致；擋下時整列停用並寫「此優惠券僅適用於◯◯」。**demo 已同步補上同樣的擋法與文案**（`couponLineHit()`）| `1f9c0d56f` |
| 購物車限購 | 改由後台設定決定，同一份禮物可放多份 | `7e4510396` |
| 禮物列數量 | 數量鎖移到後端；App 重建購物車時跳過禮物列。**demo 還是每張券一列、寫死「1 固定」，跟正式站的合併顯示不同**（見 E 區）| `0d3e56406` |
| 備援品核銷 | 改用原品的券，避免缺貨替換後整單核銷失敗 | `594a4bb0d` |
| 贈送購物金 | 原因改必填 | `20a48aac9` |
| 團購購物車 | 擋下禮物券兌換 | `9705d54b8` |
| 移除後門 | 三條停用已久的發券與建單後門已移除 | `a6c6a4a97` |

---

## E. demo 自己的內部衝突（2026-09-18 查到）

第 1、2、3 條已修，第 4 條 Glen 指示先不動。

| # | 衝突 | 狀態 |
| --- | --- | --- |
| 1 | **同一個券種的兩張券狀態不一樣**：`OR-260701-0021` 是替換品項、`OR-260701-0022` 是補貨中；會員中心同樣一張顯示原禮物、一張顯示已替換 | ✅ 已修 |
| 2 | **狀態與庫存對不上**：禮物 6 庫存 6（有貨）卻有券顯示替換品項 | ✅ 已修 |
| 3 | **現有假資料做不出合法的「補貨中」**：唯一庫存 0 的禮物 4 的備選（禮物 3）有 260 份 | ✅ 已修 |
| 4 | **購物車禮物列**：demo 每張券各一列、數量寫死「1 固定」；正式站是同一份禮物合併成一列、數量＝佔用券數、標籤寫「禮物券兌換 ×N」 | ⏸ 先不動 |
| 5 | **同一張券在兩頁的到期日不同**：`BD-BABY-0052` 在建單頁是 2026/09/20，在會員頁與會員中心是 09/09。發券日 08/26 ＋ 14 天 ＝ **09/09**，建單頁那個是錯的 | ✅ 順手修掉 |
| 6 | **券號流水超過該券種的已發張數**：券號是「券代碼 ＋ 該券種第幾張」，`CS-260715` 只發了 52 張卻有 `-0107`；`BD-BABY` 發了 170 張卻有 `-0176`、`-0338` | ⚠️ **未修，等指示**。都在 `admin-member-gift-b.html`，`CS-260715-0107` 另外出現在 `admin-order-refund-gift.html` |

### 1～3 怎麼修的

根因是**卡片狀態寫死在每一張券上**，不是從庫存推出來的。現在改成推導，三種狀態各由一份禮物負責：

| 狀態 | 禮物 | 庫存 | 券種 |
| --- | --- | --- | --- |
| 正常 | 1 寶寶果泥 隨手包 / 2 有機米餅 二入（中秋禮專用） | 84 / 12 | 寶寶生日禮、中秋限定禮 |
| 替換品項 | 6 季節果乾 隨手包 ⇒ 備選 5 有機米餅 二入（替代品專用） | 0 ⇒ 180 | 訂單回饋禮 |
| 補貨中 | 3 寶寶粥 鮮蔬雞肉，沒有備選 | 0 | 客服補償券 |

- `admin-order-create-gift-c.html` 新增 `PRESENT`（禮物庫存正典）與 `presentState()`，券只記「哪個券種的第幾張、綁哪份禮物」，畫面欄位全部推導。開發者面板的「券都在補貨中」改成把庫存當成 0 再跑同一段推導，不再是另一份寫死的資料
- `member-center-c.html`、`app-gift-list.html` 的第三張卡從「訂單回饋禮・補貨中」換成「客服補償券・補貨中」
- `admin-gift-detail.html`、`admin-gift-list.html`、`admin-gift-create.html` 的禮物 3、6 庫存改成 0
- 禮物 3（#77208）沒有設圖，順帶成為「禮物沒有圖」的示範：後台列表「無圖」、前台卡片與彈窗是灰底的「禮物圖」佔位（App 的圖原本沒有 fallback，一起補上）

規則與三種狀態的對照已寫進 `0_開發文件/05_Demo對照與施工陷阱.md` 第五節。
