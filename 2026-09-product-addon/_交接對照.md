# 交接對照 — 產線加購品設定（admin-lineaddon-setting.html / E 限定子分類）

> 建立日期：2026-09-18
> 類型：參考資料
> 適用對象：接手實作的工程師與其 AI
> 資料來源：`D:\GreenBox\react-component-export\src\pages\extraProductsSetting\`（ExtraProductSetting/Setting/SortableTable/SettingForm.tsx）、`D:\Obsidian\GreenBox\02_一般需求\202609_商品頁加購品\產線加購品_限定子分類_工程規格.md`、memory `product-type2-subcategory-mapping`

demo 是示意稿，欄位鍵與 ID 都是 demo 自由命名。工程請照下表對回正式站真名，**不要照 demo 的 JS 鍵或假 ID 去 codebase 找**。

---

## A. 現有真名（demo 已對應，工程直接抄）

真實欄位＝`product/app/cartProductDetails` 回傳的商品明細。**後端權威投影**在 `Product_Api/src/Application/Mediators/ProductDetails/Queries/GetCartItems/GetCartProductDetailsQuery.cs`（`CartProductDetailDto`）；前端型別鏡射在 `Setting.tsx` 的 `IProductDetail`。DTO 名可能≠實體欄位名（如 `DiscountPrice`←`DiscPrice`、`Stock`＝算出來的），以後端投影為準。

| demo（顯示／JS 鍵） | 真實名稱 | 類型 | 來源檔:行 |
|---|---|---|---|
| 產線頁籤（產地直送/蔬果乾貨/生鮮肉舖/粥寶寶/益菓保） | `ProductCategoryEnum`（DirectlySale/DriedFood/FrozenFood/BabyFood/EagleBabee）；欄位 `categoryType` | enum/欄位 | ExtraProductSetting.tsx:22-45、Setting.tsx:16 |
| 規格ID（demo `id`） | `productDetailId` | 欄位 | Setting.tsx:14 |
| 產品ID（demo `pid`） | `productId` | 欄位 | Setting.tsx:13 |
| 名稱（demo `name`） | `name` | 欄位 | Setting.tsx:22 |
| 規格（demo `spec`） | `detail`（規格描述）＋ `amt`（規格量）＋ `module`（單位）；顯示組成＝`fullDetailDescription` | 欄位 | Setting.tsx:20,23-24,57-59 |
| 原價（demo `price`） | `price` | 欄位 | Setting.tsx:26 |
| 優惠價（demo `disc`） | DTO `DiscountPrice` ← entity `DiscPrice`；另 `SellPrice ＝ DiscPrice ?? Price`（實際售價） | 計算/欄位 | GetCartProductDetailsQuery.cs:141-142 |
| **剩餘數量（demo `stock`）** | DTO `Stock` ＝ **`OriginAmt`（庫存量）－ `SoldAmt`（銷售量）**，clamp ≥0；**不是原始庫存** | 計算欄位 | GetCartProductDetailsQuery.cs:147 |
| （原始庫存量 / 銷售量，算剩餘用） | ProductDetail.`OriginAmt` / ProductDetail.`SoldAmt` | 欄位 | GetCartProductDetailsQuery.cs:147、147 |
| 溫層（demo `temp`） | `temperatureType`（顯示走 `text[]` map） | 欄位 | Setting.tsx:25,60；components/product/item/badge/temperatureBadge |
| 上架/下架（demo `display`） | `display` | 欄位 | Setting.tsx:37 |
| 滿額限購（demo `minPrice`） | `minimumPrice`（同產線滿額才可購買） | 欄位 | Setting.tsx:32-33 |
| 限購數量（demo `limit`） | `limit`（每單限購）；另有 `totalLimit`（產品總限購） | 欄位 | Setting.tsx:30-31 |
| VIP限購（demo `vip`） | `vipLevelId`（number[]） | 欄位 | Setting.tsx:34 |
| 首購（demo `firstBuy`） | DTO `FirstBuy` ＝ 由 `Product.ProductConstraints` 含「首購商品」推導（非 ProductDetail 上的布林欄） | 計算 | GetCartProductDetailsQuery.cs:151-153 |
| 含運（demo `shipping`） | `isShipping`（包含運費） | 欄位 | Setting.tsx:38-39 |
| 封面（demo `img`） | `coverImg` | 欄位 | Setting.tsx:16 |
| （加購品本身的子分類，比對基準） | `subCategoryType`（＝`Product.Type2`） | 欄位 | Setting.tsx:15；memory product-type2-subcategory-mapping |
| 頁面 admin-lineaddon-setting.html | React `src/pages/extraProductsSetting/`（ExtraProductSetting.tsx 進入、Setting.tsx、SortableTable.tsx、SettingForm.tsx、Descriptions.tsx） | 檔案 | 該資料夾 |
| 讀清單 | `GET product/extraProductDetailIds`（`GetExtraProductDetailIdsQuery`） | 路徑/查詢 | ExtraProductSetting.tsx:62、工程規格 §2-2/2-4 |
| 儲存（demo 儲存鈕） | `POST product/extraProductDetailIds`（`UpdateExtraProductDetailIdsCommand`） | 路徑/命令 | SortableTable.tsx:359、工程規格 §2-3/2-4 |
| 商品明細來源 | `GET product/app/cartProductDetails` | 路徑 | Setting.tsx:107 |
| APP 購物車讀加購 | `POST product/app/extraProducts`（`GetAppExtraProductsQuery`，含溫層/下架/無庫存/每產線 Take(4) 過濾） | 路徑/查詢 | 工程規格 §2-2/2-4 |

---

## B. 新功能・提案命名（正式站還沒有，待新建／工程定案）

E 限定子分類＝儲存重寫，全部依工程規格《產線加購品_限定子分類_工程規格.md》。

| demo | 建議／規格命名 | 類型 | 備註 |
|---|---|---|---|
| 產線加購品清單本體（現況一包 JSON 在 `Other.Remark`） | `ExtraProduct` 表（`ProductCategory`、`ProductDetailId`、`Sort`） | 新表 | §3-1；唯一鍵 (ProductCategory, ProductDetailId) |
| 限定子分類（demo `subs`：某加購品限定的子分類集合） | `ExtraProductSubCategory` 表・欄位 `SubCategoryType`（值＝`Product.Type2`）；空集合＝不限 | 新表/欄位 | §3-1、§3-2 |
| 排序（demo 拖曳／置頂置底／加入順序下拉） | `ExtraProduct.Sort` | 欄位 | §3-1；現況是 JSON 字串順序 |
| 儲存契約（要帶每筆子分類） | `POST extraProductDetailIds` ＝ `{ ProductCategory, Items:[{ ProductDetailId, Sort, LimitSubCategoryTypes:int[] }] }` | API 契約 | §5 新契約，取代現況「一產線一串規格ID」 |
| APP 購物車帶子分類 | request 增 `CartSubCategoryTypes: int[]`（購物車商品 `Product.Type2` 去重） | API 欄位 | §5；舊版 APP 不送＝不做子分類過濾（向下相容，§7） |
| 篩選子分類的選項來源 | 動態撈 `ProductSubCategory`（`ProductCategoryId=5`、`Type=1`、`Display=true`） | 資料來源 | §4-3；**不要寫死** |
| 隱藏下架（demo 篩選器） | 前端顯示過濾 by `display` | UI 呈現 | demo 端呈現用；APP 端排除 `!Display` 在 `GetAppExtraProductsQuery` |
| 售完＝剩餘數量 0 標紅（與下架獨立） | 判 `stock<=0`（≠ `display`） | 呈現規則 | APP 端另會排除無庫存，見 §2-2 |

---

## C. Demo 專用假資料（勿當真、勿進 codebase）

- **每條產線的子分類**（demo 寫死：蔬果乾貨＝堅果雜糧/果乾蜜餞、生鮮肉舖＝溫體肉品/鮮凍海鮮、粥寶寶＝寶寶副食、益菓保＝機能保健…）＝**捏造示意**。正式站目前實際只有 5 個子分類：**在地小農／安心時蔬／進口菁品／嚴選好物／水產直送**，且要動態撈（§2-1、§4-3）。
- **規格ID 90001+、產品ID 30001+**、20 筆產地直送商品名 ＝ demo 流水號／示意，非真實資料。
- **儲存三態文字（無改動/改動尚未儲存/儲存中）、加入順序下拉、編輯加購規則彈窗、編輯子分類彈窗** ＝ demo 互動示意，正式站現況無對應現成元件；要不要做、怎麼做由工程／營運定。

---

## D. 待工程確認的名稱落差

- **FrozenFood 的中文**：React radio 標「**生鮮肉舖**」（demo 沿用），工程規格 §2-1 寫 FrozenFood＝「冷凍」。同一個 enum、兩種中文，開工前先確認正式站該產線的正名。
- 現況 `ExtraProductConfig` 用「每產線一個寫死字串屬性」（`Product_Id_3`＝產地直送…），這些是**舊儲存的隱藏對應**，新表落地後不該再沿用，見工程規格 §2-1。
